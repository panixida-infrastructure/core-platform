{{- if and .Values.sonarqube.enabled .Values.sonarqube.externalSecrets.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.sonarqube.externalSecrets.serviceAccountName }}
  namespace: {{ .Values.sonarqube.namespace }}
  labels:
    {{- include "core-platform-workloads.labels" . | nindent 4 }}
    app.kubernetes.io/name: sonarqube
  annotations:
    argocd.argoproj.io/sync-wave: "-40"
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: {{ .Values.sonarqube.externalSecrets.secretStoreName }}
  namespace: {{ .Values.sonarqube.namespace }}
  labels:
    {{- include "core-platform-workloads.labels" . | nindent 4 }}
    app.kubernetes.io/name: sonarqube
  annotations:
    argocd.argoproj.io/sync-wave: "-30"
spec:
  provider:
    vault:
      server: {{ .Values.sonarqube.externalSecrets.openbao.server | quote }}
      path: {{ .Values.sonarqube.externalSecrets.openbao.path | quote }}
      version: {{ .Values.sonarqube.externalSecrets.openbao.version | quote }}
      auth:
        kubernetes:
          mountPath: {{ .Values.sonarqube.externalSecrets.openbao.authMountPath | quote }}
          role: {{ .Values.sonarqube.externalSecrets.openbao.role | quote }}
          serviceAccountRef:
            name: {{ .Values.sonarqube.externalSecrets.serviceAccountName }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarqube
  namespace: {{ .Values.sonarqube.namespace }}
  labels:
    {{- include "core-platform-workloads.labels" . | nindent 4 }}
    app.kubernetes.io/name: sonarqube
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  refreshInterval: {{ .Values.sonarqube.externalSecrets.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.sonarqube.externalSecrets.secretStoreName }}
    kind: SecretStore
  target:
    name: {{ .Values.sonarqube.secretName }}
    creationPolicy: Orphan
  dataFrom:
    - extract:
        key: {{ .Values.sonarqube.externalSecrets.openbao.remoteKey }}
{{- end }}
