---
all:
  children:
    managed_servers:
      hosts:
        infrastructure:
          ansible_host: "{{ lookup('env', 'SERVER_HOST') | default('81.200.158.9', true) }}"
          ansible_user: "{{ lookup('env', 'SERVER_USER') | default('root', true) }}"
          ansible_port: "{{ lookup('env', 'SERVER_SSH_PORT') | default('22', true) }}"
          ansible_ssh_private_key_file: "{{ lookup('env', 'INFRASTRUCTURE_SSH_PRIVATE_KEY_FILE') | default('~/.ssh/id_ed25519', true) }}"
          root_ssh_public_key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIF24IfpOKOfreBxvhsTICQzi/r+qvc0kQX9RIAKKwLfL infrastructure"
          ansible_python_interpreter: /usr/bin/python3
    core_platform:
      hosts:
        infrastructure:
